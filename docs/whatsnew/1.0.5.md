# What's New — v1.0.5

## ✨ 新功能

- **模板回复支持手动切语言**：模板回复弹窗新增语言下拉，可手动切换该模板已有的任意译文语言，不再只依赖评论语言自动识别。

## 🐛 修复

- **Claude CLI 未登录提示优化**：当检测到的 CLI 来自 Desktop App 内置版本（不在终端 PATH 里）时，登录提示会改为显示完整路径命令，避免终端敲 `claude` 报 command not found。
